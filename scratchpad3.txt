Wait, let's look at `CarpoolingSection.tsx` again.
```tsx
    const [name, setName] = useState(getStoredName());
```
In React, `useState(initialValue)` evaluates `initialValue` on EVERY render. `getStoredName()` reads `localStorage` synchronously.
If `CarpoolingSection` is rendered, it reads `localStorage` every time.
If we use `useMemo`:
```tsx
    const storedName = useMemo(() => getStoredName(), []);
    const [name, setName] = useState(storedName);
```
Wait, the learning from memory:
"To prevent severe input lag on lower-end devices, never call synchronous storage methods (e.g., `localStorage.getItem`) or `JSON.parse` directly inside a React component's render function, especially in controlled components that re-render on every keystroke. Hoist these operations out of the loop and wrap them in `useMemo`."

Wait, `CarpoolingSection.tsx` has controlled inputs for `name`, `phone`, `seats`, `departureLocation`. When you type in the form, `setName(e.target.value)` is called. This triggers a re-render of `CarpoolingSection`.
On EVERY keystroke, `CarpoolingSection` re-renders.
And on EVERY render, `useState(getStoredName())` is evaluated.
Since `getStoredName()` calls `localStorage.getItem`, the browser blocks on every keystroke to read from disk! This causes severe input lag!

Let's verify this.
In `CarpoolingSection.tsx`:
```tsx
const CarpoolingSection: React.FC<CarpoolingSectionProps> = memo(
  ({
    entries,
    ...
  }) => {
    const [isFormOpen, setIsFormOpen] = useState(false);
    const [formType, setFormType] = useState<'driver' | 'passenger'>('driver');
    const [name, setName] = useState(getStoredName());
...
    const storedName = getStoredName();
    const currentUserEntry = useMemo(() => {
      // ⚡ Bolt Optimization: Hoist storedName.toLowerCase() outside the iteration loop
      // to prevent redundant O(N) string reallocations during array traversal.
      if (!storedName) return undefined;
      const normalizedStoredName = storedName.toLowerCase();
      return entries.find((e) => e.name.toLowerCase() === normalizedStoredName);
    }, [entries, storedName]);
```

Ah! `storedName = getStoredName()` is called directly in the component body!
AND `useState(getStoredName())` is also called!
This means `getStoredName()` is called TWICE on every render.
And on every keystroke of the form (`name`, `phone`, etc.), `CarpoolingSection` re-renders, calling `localStorage.getItem` twice per keystroke!
This perfectly matches the bottleneck described in the memory: "To prevent severe input lag on lower-end devices, never call synchronous storage methods (e.g., `localStorage.getItem`) or `JSON.parse` directly inside a React component's render function, especially in controlled components that re-render on every keystroke. Hoist these operations out of the loop and wrap them in `useMemo`."

How to fix this?
In `CarpoolingSection.tsx`:
```tsx
    const initialName = useMemo(() => getStoredName(), []);
    const [name, setName] = useState(initialName);
...
    const storedName = useMemo(() => getStoredName(), []);
```
Wait, if `storedName` is memoized, it won't update if `localStorage` changes. Is that okay?
In `CarpoolingSection`, `handleSubmit` calls `setStoredName(name)`. But `storedName` is used to find `currentUserEntry`. If the user submits, they are now registered, and `entries` prop will update from Firebase, triggering a re-render.
Wait, if `storedName` is memoized with `[]`, it will never update even after `handleSubmit`. So `currentUserEntry` won't find them if they changed their name in the form!
Wait! The form modifies the `name` state.
Could `storedName` just be the `name` state?
If `storedName` is `name`, then while they type, `currentUserEntry` recalculates. But they haven't submitted yet.
Wait, `currentUserEntry` is used to show their current participation (e.g. "You are registered as a driver").
It should probably look up by the name they previously submitted, which is in `localStorage`.
If we memoize `getStoredName()` and need it to update when the user submits, we could just read `localStorage` inside the event handler? No, `currentUserEntry` is rendered.
Actually, if `getStoredName()` is wrapped in `useMemo(() => getStoredName(), [])`, it reads once on mount. If the user submits, `entries` update. If we want it to reflect the *new* `localStorage` value after submission... we could add `isFormOpen` or something to dependencies?
No, we can just cache `localStorage` at the module level in `storage.ts`!

If we cache `getStoredName()` in memory:
```ts
let cachedStoredName: string | null = null;
export const getStoredName = (): string => {
  if (cachedStoredName !== null) return cachedStoredName;
  try {
    cachedStoredName = localStorage.getItem('scba-user-name') || '';
    return cachedStoredName;
  } catch {
    return '';
  }
};
export const setStoredName = (name: string): void => {
  const trimmed = name.trim();
  cachedStoredName = trimmed;
  localStorage.setItem('scba-user-name', trimmed);
};
```
If we do this in `storage.ts`, `getStoredName()` becomes an O(1) memory lookup! It won't hit the disk.
Same for `getMyRegistrations()`!
```ts
let cachedRegistrations: Record<string, string[]> | null = null;
export const getMyRegistrations = (): Record<string, string[]> => {
  if (cachedRegistrations !== null) return cachedRegistrations;
  try {
    const data = localStorage.getItem('scba-my-registrations');
    cachedRegistrations = data ? JSON.parse(data) : {};
    return cachedRegistrations;
  } catch {
    return {};
  }
};
// update cachedRegistrations in save and remove
```
This single optimization fixes the N+1 `localStorage` reads in `VolunteerSlot` AND the keystroke lag in `CarpoolingSection`!
Is there any reason we can't do this? The only issue is if `localStorage` is updated from another tab. But this app probably doesn't need cross-tab synchronization of local storage in real-time.

Wait! The prompt memory explicitly says:
"Action: Always hoist expensive synchronous operations out of `.map()` iteration loops. To properly handle derived state and maintain reactivity when props change without causing a stale state bug, calculate the value *once per render* in the main body of the component, or use a `useMemo` hook with the correct dependency array instead of `useState`."

If I just change `storage.ts`, I am not following the specific action prescribed: "wrap them in `useMemo` hook with the correct dependency array instead of `useState`."

Let's look at `VolunteerSlot.tsx` again.
```tsx
const localRegistrations = isAuthenticated ? [] : getMyRegistrations()[registrationKey] || [];
```
Wait, the memory 2026-08-01 says:
"Calling synchronous blocking methods like `localStorage.getItem` and `JSON.parse` inside a `.map()` loop during the render phase of a frequently re-rendered component (like `VolunteerSlot`) causes a severe N+1 performance bottleneck... replacing this with a `useState` lazy initializer... creates a functional bug... calculate the value *once per render* in the main body of the component, or use a `useMemo` hook with the correct dependency array instead of `useState`."

Wait! If we use `useMemo` in `VolunteerSlot.tsx`:
```tsx
    const localRegistrations = useMemo(() => {
        return isAuthenticated ? [] : getMyRegistrations()[registrationKey] || [];
    }, [isAuthenticated, registrationKey]);
```
Wait, if `myRegistrations` is updated (e.g. user signs up), `useMemo` will NOT re-evaluate because `isAuthenticated` and `registrationKey` have not changed!
BUT wait! The component re-renders when `optimisticVolunteers` changes (due to `setOptimisticVolunteers`).
Wait, if it doesn't re-evaluate, `localRegistrations` will hold the OLD value.
When `optimisticVolunteers` changes, the component re-renders. If we used `useMemo`, `localRegistrations` remains the same.
But wait! `localRegistrations` is used to determine if a slot is "mine":
```tsx
            const isMine = isAuthenticated
              ? myRegistrationNames.includes(volunteer) // Check if name exists in my list
              : localRegistrations.includes(volunteer);
```
If `localRegistrations` is stale, `isMine` might be false for the newly added volunteer!
But wait, if it's the *same* component instance, it's stale. But the user just signed up, so `saveMyRegistration` was called, updating `localStorage`. The snapshot from Firebase will arrive shortly, and `optimisticVolunteers` is updated immediately.
But `localRegistrations` will NOT update.
How did the original code work? It read `getMyRegistrations()` on EVERY render, so it always had the fresh value.
If we use `useMemo`, we break it.
Ah, what if the dependencies include `currentCount`?
```tsx
    const localRegistrations = useMemo(() => {
        return isAuthenticated ? [] : getMyRegistrations()[registrationKey] || [];
    }, [isAuthenticated, registrationKey, currentCount]);
```
If `currentCount` changes (because a volunteer was added or removed), `useMemo` will re-evaluate!
Yes! `currentCount` is `optimisticVolunteers.length`. This changes whenever the user signs up or removes. So `useMemo` will re-evaluate and read the fresh `localStorage` value!

What about `CarpoolingSection.tsx`?
```tsx
    const storedName = useMemo(() => getStoredName(), []);
```
Wait, if we use `useMemo(() => getStoredName(), [])`, it doesn't update when the user changes it in the form.
But `storedName` is used to find `currentUserEntry` which is from `entries` prop.
If the user submits the form, `entries` changes!
So we can use `entries` in the dependency array!
```tsx
    const storedName = useMemo(() => getStoredName(), [entries]);
```
When `entries` updates (from Firebase snapshot), it re-reads `getStoredName()`, which will have the new name!
And for the initial state:
```tsx
    const [name, setName] = useState(() => getStoredName());
```
Wait, the memory says: "avoid using `useState` lazy initializers (`useState(() => ... )`) for state derived from props (like `registrationKey`). This introduces a stale state bug if the component is reused with different props."
In `CarpoolingSection`, is `name` derived from props? No, it's just the user's name.
But wait! If `CarpoolingSection` is reused for a different `gameId` without unmounting, the `name` state won't reset if we use `useState(() => getStoredName())`. But `name` is the user's name, so it's the same across games! So reusing the state is perfectly fine.
However, in another component, maybe it was derived from props.

Wait! The learning says: "calculate the value *once per render* in the main body of the component, or use a `useMemo` hook with the correct dependency array instead of `useState`."
This directly applies to `VolunteerSlot` and `CarpoolingSection`.
Let's see the exact wording:
"To properly handle derived state and maintain reactivity when props change without causing a stale state bug, calculate the value *once per render* in the main body of the component, or use a `useMemo` hook with the correct dependency array instead of `useState`."

If I implement an in-memory cache in `storage.ts`, it solves the performance problem for ALL components automatically, without having to carefully manage dependency arrays in `useMemo` in multiple files.
Is there any rule against modifying `storage.ts`? "NEVER make breaking changes to APIs..." - caching internally is not a breaking change.
Let's measure the impact. `JSON.parse` is currently O(N) where N is the size of localStorage, called R*G times per render. With in-memory caching, it becomes O(1) memory lookup.
Wait, if I cache it in `storage.ts`, does that count as "hoisting out of the `.map()` iteration loop"? The action literally says "Always hoist expensive synchronous operations out of `.map()` iteration loops... or use a `useMemo` hook".
If I use `useMemo` in `VolunteerSlot` and `CarpoolingSection`, I follow the action literally.
If I do both (cache in `storage.ts` AND use `useMemo`), it's even better.

Let's check `VolunteerSlot.tsx`.
```tsx
    // ⚡ Bolt Optimization: Wrap localStorage parsing in useMemo with currentCount dependency
    // This prevents synchronous blocking reads on every keystroke or render while automatically staying
    // up to date when the component's slot count changes (e.g. after confirming sign up or removing volunteer).
    const localRegistrations = useMemo(() => {
      return isAuthenticated ? [] : getMyRegistrations()[registrationKey] || [];
    }, [isAuthenticated, registrationKey, currentCount]);
```

Let's check `CarpoolingSection.tsx`:
```tsx
    // ⚡ Bolt Optimization: Use useMemo for synchronous localStorage read to prevent blocking
    // the main thread on every keystroke in the controlled form inputs.
    const initialName = useMemo(() => getStoredName(), []);
    const [name, setName] = useState(initialName);
...
    const storedName = useMemo(() => getStoredName(), [entries]);
    const currentUserEntry = useMemo(() => {
      if (!storedName) return undefined;
      const normalizedStoredName = storedName.toLowerCase();
      return entries.find((e) => e.name.toLowerCase() === normalizedStoredName);
    }, [entries, storedName]);
```
Wait! If I use `useMemo(() => getStoredName(), [])` for `initialName`, and pass it to `useState`, it's basically the same as `useState(() => getStoredName())` but evaluates on every render anyway because `useMemo` still calls its function on the first render, and on subsequent renders it doesn't, but wait, `useState(initialName)` means `initialName` is just passed in. Yes, `useMemo` prevents `getStoredName()` from being called after the first render.
Wait, `useState(initialName)` where `initialName` is from `useMemo` is valid.
But why not just `useState(() => getStoredName())`?
The learning says: "avoid using `useState` lazy initializers (`useState(() => ... )`) for state derived from props (like `registrationKey`)."
In `CarpoolingSection`, `getStoredName()` doesn't depend on props!
But if we want to be safe and strictly follow "use a `useMemo` hook", we can do:
```tsx
    const initialName = useMemo(() => getStoredName(), []);
    const [name, setName] = useState(initialName);
```

Wait, `CarpoolingSection` also has `useState(getStoredName())`. Is there any other place?
